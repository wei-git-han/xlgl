<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.css.app.xlgl.zzxl.dao.GroupTrainDao">

	<select id="queryObject" resultType="com.css.app.xlgl.zzxl.entity.GroupTrain">
		select *,CASE type when '0' then '未发布' else '已发布' end as gt_type,CONCAT(CONCAT(start_date,' ',start_time),'~',CONCAT(end_date,' ',end_time)) as plan_time  from T_GROUP_TRAIN where ID = #{value}
	</select>

	<select id="queryList" resultType="com.css.app.xlgl.zzxl.entity.GroupTrain">
		select *,CASE type when '0' then '未发布' else '已发布' end as gt_type,CONCAT(CONCAT(start_date,' ',start_time),'~',CONCAT(end_date,' ',end_time)) as plan_time  from T_GROUP_TRAIN 
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from T_GROUP_TRAIN 
	</select>
	 
	<insert id="save" parameterType="com.css.app.xlgl.zzxl.entity.GroupTrain">
		insert into T_GROUP_TRAIN
		(
			"ID", 
			"TITLE", 
			"CONTENT", 
			"GOAL", 
			"CREATED_USER_ID", 
			"CREATED_USER", 
			"CREATED_TIME", 
			"START_DATE", 
			"END_DATE", 
			"START_TIME", 
			"END_TIME", 
			"APPLY_END_TIME", 
			"PLACE", 
			"TYPE", 
			"TRAIN_DEPT", 
			"TRAIN_DEPT_ID"
		)
		values
		(
			#{id}, 
			#{title}, 
			#{content}, 
			#{goal}, 
			#{createdUserId}, 
			#{createdUser}, 
			#{createdTime}, 
			#{startDate}, 
			#{endDate}, 
			#{startTime}, 
			#{endTime}, 
			#{applyEndTime}, 
			#{place}, 
			#{type}, 
			#{trainDept}, 
			#{trainDeptId}
		)
	</insert>
	 
	<update id="update" parameterType="com.css.app.xlgl.zzxl.entity.GroupTrain">
		update T_GROUP_TRAIN 
		<set>
			<if test="title != null">"TITLE" = #{title}, </if>
			<if test="content != null">"CONTENT" = #{content}, </if>
			<if test="goal != null">"GOAL" = #{goal}, </if>
			<if test="createdUserId != null">"CREATED_USER_ID" = #{createdUserId}, </if>
			<if test="createdUser != null">"CREATED_USER" = #{createdUser}, </if>
			<if test="createdTime != null">"CREATED_TIME" = #{createdTime}, </if>
			<if test="startDate != null">"START_DATE" = #{startDate}, </if>
			<if test="endDate != null">"END_DATE" = #{endDate}, </if>
			<if test="startTime != null">"START_TIME" = #{startTime}, </if>
			<if test="endTime != null">"END_TIME" = #{endTime}, </if>
			<if test="applyEndTime != null">"APPLY_END_TIME" = #{applyEndTime}, </if>
			<if test="place != null">"PLACE" = #{place}, </if>
			<if test="type != null">"TYPE" = #{type}, </if>
			<if test="trainDept != null">"TRAIN_DEPT" = #{trainDept}, </if>
			<if test="trainDeptId != null">"TRAIN_DEPT_ID" = #{trainDeptId}</if>
		</set>
		where ID = #{id}
	</update>
	
	<delete id="delete">
		delete from T_GROUP_TRAIN where ID = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from T_GROUP_TRAIN where ID in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>