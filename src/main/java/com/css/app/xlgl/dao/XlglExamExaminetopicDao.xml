<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.css.app.xlgl.dao.XlglExamExaminetopicDao">

	<select id="queryObject" resultType="com.css.app.xlgl.entity.XlglExamExaminetopic">
		select * from XLGL_EXAM_EXAMINETOPIC where id = #{value}
	</select>

	<select id="queryList" resultType="com.css.app.xlgl.entity.XlglExamExaminetopic">
		select * from XLGL_EXAM_EXAMINETOPIC 
		where 1=1 
		<if test="examineId != null and examineId !=''">
			  and EXAMINE_ID = #{examineId}
		</if>
		<if test="makeUpId != null and makeUpId !=''">
			  and MAKEUP_ID = #{makeUpId}
		</if>
		<if test="makeUpStatus != null and makeUpStatus !=''">
			  and MAKEUP_STATUS = #{makeUpStatus}
		</if>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from XLGL_EXAM_EXAMINETOPIC 
		<if test="examineId != null and examineId !=''">
			  and EXAMINE_ID = #{examineId}
		</if>
		<if test="makeUpStatus != null and makeUpStatus !=''">
			  and MAKEUP_STATUS = #{makeUpStatus}
		</if>
	</select>
	 
	<insert id="save" parameterType="com.css.app.xlgl.entity.XlglExamExaminetopic">
		insert into XLGL_EXAM_EXAMINETOPIC
		(
			"id", 
			"EXAMINE_ID", 
			"SUBJECT_ID", 
			"TOPIC_ID",
			"MAKEUP_ID", 
			"TOPIC_TYPE", 
			"TOPIC_COLUMN", 
			"TOPIC_OPTION", 
			"TOPIC_RESULT", 
			"FRACTIONAL_NUMBER", 
			"CREATE_USER", 
			"CREATE_DATE", 
			"UPDATE_USER", 
			"UPDATE_DATE",
			"SORT"
		)
		values
		(
			#{id}, 
			#{examineId}, 
			#{subjectId}, 
			#{topicId}, 
			#{makeUpId},
			#{topicType}, 
			#{topicColumn}, 
			#{topicOption}, 
			#{topicResult}, 
			#{fractionalNumber}, 
			#{createUser}, 
			#{createDate}, 
			#{updateUser}, 
			#{updateDate},
			#{sort}
		)
	</insert>
	 
	<update id="update" parameterType="com.css.app.xlgl.entity.XlglExamExaminetopic">
		update XLGL_EXAM_EXAMINETOPIC 
		<set>
			<if test="examineId != null">"EXAMINE_ID" = #{examineId}, </if>
			<if test="subjectId != null">"SUBJECT_ID" = #{subjectId}, </if>
			<if test="topicId != null">"TOPIC_ID" = #{topicId}, </if>
			<if test="makeUpId != null">"MAKEUP_ID" = #{makeUpId}, </if>
			<if test="makeUpStatus != null">"MAKEUP_STATUS" = #{makeUpStatus}, </if>
			<if test="topicType != null">"TOPIC_TYPE" = #{topicType}, </if>
			<if test="topicColumn != null">"TOPIC_COLUMN" = #{topicColumn}, </if>
			<if test="topicOption != null">"TOPIC_OPTION" = #{topicOption}, </if>
			<if test="topicResult != null">"TOPIC_RESULT" = #{topicResult}, </if>
			<if test="fractionalNumber != null">"FRACTIONAL_NUMBER" = #{fractionalNumber}, </if>
			<if test="createUser != null">"CREATE_USER" = #{createUser}, </if>
			<if test="createDate != null">"CREATE_DATE" = #{createDate}, </if>
			<if test="updateUser != null">"UPDATE_USER" = #{updateUser}, </if>
			<if test="updateDate != null">"UPDATE_DATE" = #{updateDate},</if>
			<if test="sort != null">"SORT" = #{sort}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from XLGL_EXAM_EXAMINETOPIC where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from XLGL_EXAM_EXAMINETOPIC where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>
	
	<insert id="saveBatch" parameterType="com.css.app.xlgl.entity.XlglExamExaminetopic">
		insert into XLGL_EXAM_EXAMINETOPIC
		(
			"id", 
			"EXAMINE_ID", 
			"SUBJECT_ID", 
			"TOPIC_ID", 
			"MAKEUP_ID",
			"MAKEUP_STATUS",
			"TOPIC_TYPE", 
			"TOPIC_COLUMN", 
			"TOPIC_OPTION", 
			"TOPIC_RESULT", 
			"FRACTIONAL_NUMBER", 
			"CREATE_USER", 
			"CREATE_DATE", 
			"UPDATE_USER", 
			"UPDATE_DATE"
		
		)
		values
		<foreach collection="list" item="item" index="index"  separator="," >
		(
			#{item.id}, 
			#{item.examineId}, 
			#{item.subjectId}, 
			#{item.topicId},
			#{item.makeUpId}, 
			#{item.makeUpStatus},
			#{item.topicType}, 
			#{item.topicColumn}, 
			#{item.topicOption}, 
			#{item.topicResult}, 
			#{item.fractionalNumber}, 
			#{item.createUser}, 
			#{item.createDate}, 
			#{item.updateUser}, 
			#{item.updateDate}
		
		)
		</foreach>
	</insert>
	
	<select id="findCountBySubjectId" resultType="com.css.app.xlgl.dto.XlglExamExaminetopicDto">
		select 
			topic_Type as topicType,
			count(topic_Type)as typeCount,
			sum(FRACTIONAL_NUMBER)as numberAll,
			round(avg(FRACTIONAL_NUMBER)) as fractionalNumber
		from 
		XLGL_EXAM_EXAMINETOPIC 
		where 1=1
		<if test="examineId != null ">
			and EXAMINE_ID = #{examineId}
		</if>
		<if test="makeUpStatus != null and makeUpStatus !=''">
			  and MAKEUP_STATUS = #{makeUpStatus}
		</if>
		GROUP BY topic_Type;
	</select>
	
	<select id="findListBySubjectId" resultType="com.css.app.xlgl.entity.XlglExamExaminetopic">
		select * from XLGL_EXAM_EXAMINETOPIC 
		where 1=1
		<if test="examineId != null ">
			and EXAMINE_ID =#{examineId}
		</if>
		<if test="makeUpStatus != null and makeUpStatus !=''">
			  and MAKEUP_STATUS = #{makeUpStatus}
		</if>
	</select>
	<delete id="deleteByExamineId">
		delete from XLGL_EXAM_EXAMINETOPIC where EXAMINE_ID =#{examineId} 
		<if test="makeUpStatus != null and makeUpStatus !=''">
			  and MAKEUP_STATUS = #{makeUpStatus}
		</if>
	</delete>

</mapper>